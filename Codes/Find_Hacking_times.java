import java.io.*;
import java.util.Scanner;
import java.math.*;
public class Find_Hacking_times {
  public static void main (String[] args){
    Scanner input = new Scanner (System.in);
    System.out.println( "How long to tyoe one character" );
    // conver to seconds baseded on first two parameters 
    System.out.println("is this in milli seconds????");
    String in_ms;
    float _ms = input.nextLine();
    in_ms = user_input.nextInt( " " );
    int user_secs=input.nextInt(); 
    if( in_ms = "yes || true || y"){
      double secs_covert = secs * 1000  ;
    }
    int msecs=input.nextInt();
    //Calculate number of possibilities 
    double numPos = (" ");
    //calulate total times 
    double totHack= totSecs * numPos ;
    System.out.println("How many bits");
    double bits = input.nextInt();
    double totHack = input.newDouble ();
    if (totHack > 31539456) {
     double theYears = Math.floor(totHack/31539456);
     System.out.println("%d years" , theYears);
     totHack=totHack -(theYears *31539456);
    }
     if(totHack > 2628288) {
      double theMonths = Math.floor(totHack/2628288);
      System.out.printlm ("%d months", theMonths);
      totHack = totHack - (theMonths * 22628288); 
      //totHack = 1056,064
    }
    if(totHack> 0){ 
     double secs=math.floor(totHalk/1);
      System.out.println( "and %d seconds" , theSeconds);
      totHank= tothank -(theMonths );
      //totHank == 1,056,064
    }     
 }
 } 
   